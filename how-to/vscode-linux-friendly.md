# Making VSCode friendly to Linux

## User settings

Save the following settings in the file at ~/.config/Code/User/settings.json

```json
{
    "terminal.integrated.sendKeyBindingsToShell": true,
    "terminal.integrated.allowChords": false
}
```
